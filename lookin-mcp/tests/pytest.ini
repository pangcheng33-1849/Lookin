[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
markers =
    functional: 功能主链路测试（F-xxx）
    exception: 异常与错误码测试（E-xxx）
    performance: 性能基线测试（P-xxx）
    stability: 稳定性回归测试（S-xxx）
    manual: 手工验证测试（默认跳过）
